# Configurações de Ambiente para a Electoral API

# API Server Configuration
PORT=3001
HOST=0.0.0.0
NODE_ENV=development

# Database Configuration (para fases futuras)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/electoral_db?schema=public"

# Redis Configuration (para cache/rate limiting nas fases futuras)
REDIS_URL="redis://localhost:6379"
